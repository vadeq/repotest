package zws.datasource.intralink.op.workspace;/*
DesignState - Design Compression Technology
@author: athakur
@version: 1.0
Created on September 24, 2004, 9:49 AM
Copywrite (c) 2003 Zero Wait-State Inc. All rights reserved */

public class ASMPRT2IDF extends ProEConverter {
  protected String getTypeExtention() { return "emn"; }
  protected String getTargetOutputType() { return "IDF"; } 
  
  protected void finishExecution() throws Exception {
    /*
    File dumpFile = new File(workingDir, FileNameUtil.convertType(getComponentName(), getTypeExtention()));
    File dummy= new File(workingDir, "123.iges");  //+++ temporary for iges stub
//    for (int i=0; i<1000000; i++) if (0==i%25000) {}//Logwriter.printOnConsole(i/250000);
    if (!dummy.exists()) //+++ temporary for iges stub
      throw new Exception("Conversion to " + getTypeExtention() + " Failed: dummy file not created ");
//    for (int i=0; i<1000000; i++) if (0==i%25000) {}//Logwriter.printOnConsole(i/250000);
    if (!dummy.renameTo(dumpFile)) //+++ temporary for iges stub
      throw new Exception("Conversion to " + getTypeExtention() + " Failed: dummy file not renamed");
//    for (int i=0; i<1000000; i++) if (0==i%25000) {}//Logwriter.printOnConsole(i/250000);
    File outFile = new File(getBinaryOutputPath(), getBinaryFilename());
    if (outFile.exists()) outFile.delete();
    if (!dumpFile.renameTo(outFile))
      throw new Exception("Conversion to " + getTypeExtention() + " Failed");
     */
  }
}
